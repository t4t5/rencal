[package]
name = "rencal-plugin-contract"
version = "0.1.0"
edition = "2024"

[dependencies]
semver = "1"
serde = { version = "1", features = ["derive"] }
specta = { version = "=2.0.0-rc.22", features = ["derive"] }
toml = "0.8"
