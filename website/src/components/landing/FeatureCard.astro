---
interface Props {
  title: string
  description: string
}

const { title, description } = Astro.props
---

<div class="flex flex-col gap-4">
  <h3 class="uppercase text-xl">{title}</h3>
  <p class="text-[15px]">{description}</p>
</div>
